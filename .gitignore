# Arquivos de build / locais
*.iml
.gradle/
build/
out/
local.properties
.idea/
.DS_Store

# Estado local das ferramentas (sessões do compilador Kotlin 2.x)
.kotlin/

# Artefatos gerados pelo Gradle / jpackage
dist/
*.jar
*.zip
*.msi
*.exe
*.hprof

# …menos o wrapper do Gradle: sem ele um clone novo não consegue rodar o
# `gradlew.bat` (e o projeto não se reconstrói a partir do repositório).
!gradle/wrapper/gradle-wrapper.jar

# Logs locais (o app também grava logs em %LOCALAPPDATA%)
*.log
